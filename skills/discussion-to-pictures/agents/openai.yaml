version: 1
display_name: Discussion To Pictures
short_description: Turn the current discussion into picture-group CRUD actions via the local content-admin MCP server.
default_prompt: Convert the current discussion into structured picture-group changes for the 2025-blog-public repository. Use only the local content-admin MCP server, follow read -> resolve id -> mutate -> verify -> summarize, default to Chinese, keep media URL-first, reject local-file-only input, never guess ambiguous targets, and only delete when I explicitly asked for deletion.
