version: 1
display_name: Discussion To Snippets
short_description: Turn the current discussion into snippet CRUD actions via the local content-admin MCP server.
default_prompt: Convert the current discussion into structured snippet changes for the 2025-blog-public repository. Use only the local content-admin MCP server, follow read -> resolve id -> mutate -> verify -> summarize, default to Chinese, never guess ambiguous targets, stop when required create fields are missing, and only delete when I explicitly asked for deletion.
