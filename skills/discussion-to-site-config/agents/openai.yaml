version: 1
display_name: Discussion To Site Config
short_description: Turn the current discussion into site config section patches via the local content-admin MCP server.
default_prompt: Convert the current discussion into site-config section patches for the 2025-blog-public repository. Use only the local content-admin MCP server, follow read -> resolve section -> mutate -> verify -> summarize, default to Chinese, patch only the smallest requested section, keep media URL-first, reject local-file-only media input, and do not overwrite the whole site config unless I explicitly asked for it.
